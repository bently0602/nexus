// The app's display name. Used in the titlebar, About dialog, and window title.
// Rename this (plus electron-builder's appId/productName in package.json) for a new project.
export const APP_NAME = "App Template";

// Shown as the eyebrow above the app name in the About dialog, and in its footer copyright.
export const APP_ORGANIZATION = "Umpecca";

// One-line description under the app name in the About dialog.
export const APP_TAGLINE = "A frameless Electron + React + Vite + TypeScript desktop template.";
